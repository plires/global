import styles from './hero-contacto.module.css'

const HeroContacto = () => {
  return (
    <section className={`${styles.heroContacto}`}>
      <h1>Conectá con nosotros</h1>
      <h2>Estamos a un mensaje de distancia</h2>
    </section>
  )
}
export default HeroContacto
